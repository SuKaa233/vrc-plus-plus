# 安全策略

## 支持范围

当前仅维护最新测试版本。`0.9.0-beta.1` 尚未作为稳定版发布，真实账号写操作、正式签名和线上自动更新仍处于发布前验证阶段。

## 报告安全问题

请不要在公开 Issue 中提交漏洞细节、账号、Cookie、2FA、Pipeline token、邮箱、位置、实例 nonce、数据库或原始日志。请通过仓库所有者提供的私密联系方式报告，并包含：

- 受影响版本和 Windows 版本；
- 最小复现步骤与预期/实际结果；
- 已脱敏的日志或截图；
- 影响范围和可能的缓解措施。

收到报告后应先确认影响和复现条件，再讨论修复及披露时间。在修复发布前，请避免公开可直接利用的细节。

## 本机数据

- 正式数据默认位于 `%AppData%\VRC++`；开发数据位于仓库 `.data`；
- 密码不落盘，Cookie 使用 Windows CurrentUser DPAPI 加密；
- `.data`、`.env*`、日志、数据库、构建产物和参考仓库均被 Git 忽略；
- 提交前仍应人工检查 `git diff --cached`，防止截图、诊断导出或个人信息误入仓库。

如果怀疑会话泄露，请先在 VRChat 官方渠道终止相关会话并更改凭据，再保留脱敏证据用于排查。
