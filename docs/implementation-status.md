# 实现状态批注

> 最后更新：2026-08-16  
> 当前版本：`0.9.0-beta.2`
> 状态图例：✅ 已实现并验证；🟡 已实现主体但验证或能力不完整；⬜ 未实现；⛔ 主动暂缓。

> 2026-08-16 P1 增强批注：好友档案 2.0、每日最多 5 条本机简报、好友聚焦回放、交汇窗口对比、共同移动链、覆盖度地图、世界回忆统计、朋友圈每日快照以及实例加入状态分类已完成。所有推导继续显示本机观测边界，不新增无人值守扫描或批量写操作。

## 总览

| 模块 | 状态 | 批注 |
|---|---|---|
| Vue Web UI | 🟡 | 已完成第二轮可读性改版：提升字号和头像密度，历史页以昵称/头像为主、ID 为次级信息；已接入公共实例、VRChat 收藏、邀请、游戏日志和更新状态。复杂筛选与静态品牌资源仍待后续。 |
| Go 本机网关 | 🟡 | 已有静态资源托管、本地 API、优雅退出、WebView2 桌面窗口与浏览器回退、Windows 单实例守卫和托盘；关闭窗口后网关保留，托盘或重复双击可重新唤醒。代码和单实例测试已通过，仍需干净 Windows 安装程序烟测。 |
| 本地 API 防护 | ✅ | 仅回环地址访问、Origin 校验、写请求 CSRF、CSP 和安全响应头已实现。 |
| SQLite | ✅ | 已实现 WAL、migration、账号档案、安全会话、实体快照、好友整理、世界本机收藏、30 天脱敏活动事件，以及共同好友扫描元数据和去重边表。 |
| 国内网络配置 | 🟡 | 已实现跟随系统、强制直连、本机 HTTP、本机 SOCKS5 四种模式，并统一应用到 REST、Pipeline 和图片；仍需不同地区/运营商实测。 |
| 图片本地缓存 | 🟡 | 页面不再直接访问境外图片，网关仅允许 VRChat/VRC CDN HTTPS 域名，限制 16 MiB 和栅格格式并写入本地磁盘；设置页可查看占用并只清理媒体文件；LRU 和容量设置尚未实现。 |
| Windows DPAPI | ✅ | Cookie 会话使用 CurrentUser DPAPI 加密后落库，密码不落库。 |
| VRChat 诊断 | 🟡 | `/config`、`/health` 探针、超时、403/429 分类与 30 秒结果缓存已实现；设置页已分层展示数据库、REST、会话、Pipeline、网络模式和媒体缓存；尚未加入 CDN/DNS/TLS 独立探针。 |
| 登录/2FA | 🟡 | Basic 登录、TOTP/邮件/恢复码、Cookie 持久化与恢复代码已实现；仍需真实测试账号完成端到端烟测。 |
| 退出 | 🟡 | 本地 Cookie 和加密会话清理已实现；上游 logout 失败时采用本地安全退出，尚未用真实账号验证。 |
| 限流与 GET 合并 | ✅ | 已实现每客户端平均 1 req/s、短突发 5 的 token bucket、429 冷却和相同端点 in-flight GET 合并。 |
| Pipeline WebSocket | 🟡 | 已实现 token 获取、WebSocket、指数退避、token 日志遮盖、通用事件解码和本地 SSE；缺真实账号长连接验证与事件语义归一化。 |
| 好友/世界/实例 | 🟡 | 已实现好友、用户详情、共同好友、世界搜索/详情、本机收藏与 VRChat 上游收藏；世界详情解析公开实例摘要，按需读取实例详情，并可逐人确认发送邀请、向单个好友发送 Boop 或启动加入。完整实例成员列表未实现。 |
| 共同好友关系网 | 🟡 | 已实现本机持久化、缓存优先详情读取、20 人顺序扫描、停止、403/404 未共享标记、边去重和覆盖率；默认只扫描未扫描或超过 7 天的数据；SVG 支持头像降级、缩放、平移、拖动、碰撞避让，并按账号保存布局；缺真实账号限流验证，大好友量布局仍需持续性能实测。 |
| 本机活动历史 | 🟡 | Pipeline 与 VRChat 游戏日志事件脱敏后保留 30 天；昵称通过当前好友快照回填，界面展示真实头像、事件来源、常见好友和活跃分布。不保存原始日志正文或 nonce；共同会话时长待补。 |
| 通知与请求 | 🟡 | 已实现通知读取及逐项确认后的已读、隐藏、接受好友请求；无自动响应、批量操作或群发邀请；真实通知类型兼容性待验证。 |
| 游戏日志监听 | 🟡 | 已实现轮询最新 `output_log_*.txt`、轮转识别、玩家进入/离开、世界和位置事件解析；真实本机已识别日志文件，解析器测试通过，仍需 VRChat 正在运行时验证连续追加与轮转。 |
| 安装程序/签名更新 | 🟡 | Inno Setup 安装程序、启动自动检查、双源清单、后台下载、Windows 签名验证、静默覆盖安装和自动重启已接入；可信证书和真实线上升级验收尚未完成。 |
| Photon/资产下载/批量 Bot | ⛔ | 按设计主动不实现。 |

## 阶段批注

### 阶段 0：技术探针

- 🟡 `/config`、`/health`：诊断代码已实现；当前开发环境实测为 Cloudflare 403，程序会标记“部分受限”而不是误报账号封禁。
- 🟡 登录与三类 2FA：实现完成，缺真实账号验证。
- ✅ User-Agent：发行默认值已内置项目标识和联系邮箱，用户无需额外配置；开发环境可按需覆盖。
- 🟡 Cookie 重启复用：DPAPI + SQLite 实现完成，缺真实 Cookie 端到端验证。
- 🟡 Pipeline 稳定运行 2 小时：连接与重连代码已实现，尚未用真实账号长稳验证。
- 🟡 VRChat 游戏日志样本和轮转：解析和轮转代码已实现，已识别真实日志目录；缺游戏运行中的连续追加验证。

阶段 0 尚未满足退出条件，不能标记完成。

### 阶段 1：工程骨架

- ✅ `apps/web`、`apps/gateway` 结构。
- ✅ Go 内嵌 Vue 构建产物。
- 🟡 本地端口：默认固定 `47831`，已仅监听回环；随机端口和启动器传递尚未实现。
- ✅ Origin 校验与 CSRF。
- ✅ SQLite migration、结构化日志和诊断页。
- ✅ 前端 JS/CSS/图标完全内嵌，运行时不依赖 Google Fonts、公共 npm CDN 或 GitHub 页面。
- ✅ 浅色/深色主题、跟随系统初始偏好和浏览器本机持久化；存储被禁用时仍可临时切换。
- 🟡 自动化：已有本地 build/test 脚本；尚未配置远程 CI。

阶段 1 约完成 80%，剩余随机端口/启动器与 CI。

### 阶段 2：可用 MVP

- 🟡 登录、2FA、会话恢复。
- 🟡 当前用户业务页、好友、世界：好友详情、公共实例摘要、本机与 VRChat 双收藏、指定实例详情、逐人邀请已实现；完整成员信息待补。
- 🟡 共同好友关系网：只基于当前账号可见的共同好友结果建立“已观察连线”，每批最多 20 人、串行且可停止；缺边不等于没有好友关系，数据只保存在本机。
- 🟡 Pipeline 与好友状态合并：浏览器收到好友类事件后会节流校正好友快照；尚未按每类事件做字段级增量归并。
- 🟡 业务搜索：世界搜索与统一入口已实现；本地好友/整理信息/世界快照可直接检索，`usr_` / `wrld_` ID 可按需读取；instance location 解析待补。
- 🟡 图片磁盘缓存：安全代理、落盘、占用统计与仅媒体清理 UI 已完成，缺 LRU、容量上限和尺寸分级。
- 🟡 本地备注：备注、分组、颜色和标签已实现并持久化；导入导出与关系网标签筛选待补。
- 🟡 关系网持续使用：布局按账号持久化、7 天过期增量扫描已实现；圈层发现和按圈筛选待补。

阶段 2 已形成只读数据闭环，但仍不能视为完整替代 VRCX。

## 已验证与未验证边界

### 2026-08-15 关系网交互与视觉改版

- ✅ 默认隐藏孤立点；用户可在工具栏手动切换为显示全部，选择状态按当前账号保存在浏览器本机。
- ✅ 增加“添加好友”选择器：支持按显示名称或 `usr_` ID 查找，单次最多选择 20 位，可只加入画布或加入后立即串行扫描。
- ✅ 批量扫描按钮在执行前显示本次实际候选人数；页面明确说明新增连线数量只有在 VRChat 返回后才能确定，不提供虚假预估。
- ✅ 增加关系网全屏模式、缩放、平移、节点拖动和自动布局恢复；全屏只改变本地显示，不触发额外上游请求。
- ✅ 拖动性能改为“图数据变化时计算布局，指针移动时只更新位置”，并使用 `requestAnimationFrame` 合并高频移动事件。
- ✅ 虚拟画布随节点数量扩展，自动布局继续执行碰撞分离；300 节点、2000 条边压力测试约 410ms，节点间距断言通过。
- ✅ 增加“核心圈”筛选，阈值按当前已连接节点的平均连接数动态计算；手动固定的好友不会被核心圈或孤立点筛选误隐藏。
- ✅ 扫描结束后对比扫描前后快照，反馈新增已扫描节点、连线和进入连接区的好友数；相关差量计算已有单元测试。
- ✅ 关系网页面改为桌面地图工具结构，移除四张嵌套统计卡；全局圆角、阴影、强调色和侧栏选中态同步收敛。
- ✅ 好友头像优先使用用户自定义 `userIcon`，并自动转换成 VRChat 256px 图片版本，不再误优先展示当前 Avatar 的默认人偶缩略图，也避免下载数 MB 原图；总览首屏头像和世界封面改为 eager，其余列表继续 lazy。
- ✅ 关系网节点增加固定透明命中圆、70ms 离开缓冲，并在聚焦期间禁用无关节点命中；去除节点透明度动画，避免头像重叠处悬停焦点反复跳转造成闪烁。
- ✅ 修正 Edge 下边缘好友悬停时整块关系图黑屏：DOM 取证确认不存在遮罩层，问题来自同时改变数百个带裁剪头像的 SVG 组透明度和命中状态造成合成层重绘；现彻底取消批量压暗与命中切换，只为当前头像、邻居和直连线添加强调样式。
- ✅ 悬停切换增加 90ms 新目标迟滞和 110ms 离开缓冲；鼠标在重叠头像边界短暂往返时保持原焦点，稳定停到新头像后才切换关系高亮。
- ✅ 在不修改头像组透明度的前提下恢复清晰焦点：只降低非直连线可见度，当前头像显示双层强调光环，直接邻居显示次级光环和昵称，背景头像始终保持完整渲染。
- ✅ 关系网焦点切换改为预构建邻接/连线索引，悬停时只更新当前好友实际关联的 DOM 连线，不再让 Vue 对全部 1893 条线执行 class diff；切换迟滞由 90ms 降至 45ms，离开缓冲降至 80ms。真实 Edge 在 161 节点、1893 连线下连续切换 13 个焦点，平均 68.4ms、最大 73.5ms（其中固定迟滞 45ms）。
- ✅ 390×844 窄屏检查已消除页面级横向滚动；移动导航、关系网筛选和画布控制可在单列布局中使用。
- 🟡 已使用真实账号数据确认旧版 186 个节点会发生明显中心堆叠；新版真实登录态截图和连续拖动手感仍需在重启后的 Edge 中复核。

### 已验证

- `npm run typecheck`、Vitest 10 项测试、Go 全包测试均通过；新增覆盖世界收藏、实例、通知操作、本机历史脱敏/统计/清除，以及所有新写路由的 CSRF；原有主题、关系图、好友/世界/Pipeline 与缓存测试继续通过；
- 生产前端构建和 Windows 单 EXE 构建通过，最终 EXE 约 11.5 MiB；
- Microsoft Edge 无头烟测已检查浅色/深色桌面 1440×1000 以及浅色移动端 390×844，未发现横向溢出或主题变量缺失；
- 使用本地模拟 VRChat 上游完成已登录控制台烟测：好友卡成功打开详情，资料/世界/共同好友渲染完成，并检查浅色、深色桌面和 390×844 移动端抽屉；
- 本机 HTTP 烟测：`/`、`/bootstrap`、内嵌状态文档均返回 200，数据库 ready；
- 未带 CSRF 的写请求返回 403；占位 `.invalid` User-Agent 会被本机网关拒绝，发行默认值已改为有效联系邮箱；
- 本地 API 的安全中间件使用单元测试验证；
- DPAPI 往返仅在 Windows 本机测试；
- SQLite migration 和会话 CRUD 使用临时数据库测试。
- 网络配置持久化、四种模式参数校验、本机代理限制以及 Pipeline Transport 复用已通过 Go 测试；
- 图片代理域名白名单和拒绝 HTTP/凭据/伪后缀域名已通过 Go 测试；本机烟测确认非白名单图片返回 400；
- CSP 已收紧为 `img-src 'self' data:`，浏览器不能绕过本机网关直接读取境外图片。
- 使用当前已恢复的真实会话只读验证：186 位好友均有昵称；历史前 20 条全部回填昵称，截图中原裸 ID 已显示为 `ArcticArua`、`MaverickmanJr`；67 个 VRChat 世界收藏和 2 个收藏分组读取成功；热门世界返回 20 个公开/群组公开实例摘要；未执行收藏增删或邀请写操作。
- 游戏日志状态读取为 `watching`，识别到本机 `output_log_2026-08-08_23-06-44.txt`；Go 解析器覆盖玩家进入和实例位置且会去除 nonce。
- 双源更新使用 `httptest` 验证首源失败后回退第二源；Windows 自替换助手改为独立临时副本，避免占用待替换主程序。

### 未验证

- 真实 VRChat 账号密码登录；
- TOTP、邮件验证码和恢复码真实流程；
- Cloudflare 允许访问的中国实际用户线路；
- Pipeline 真实长连接及两小时稳定性；
- 中国移动/联通/电信及常见本机代理的实线路验证；
- 收藏增删、发送邀请等真实上游写操作；
- 高频使用下是否会遇到 429；
- 共同好友关系网在真实账号、未共享用户和数百好友规模下的覆盖率与布局性能；
- 安装/卸载脚本的真实干净机器验证、线上双镜像更新、可信证书签名和托盘长时间常驻。

任何未验证项都不应在发布说明中表述为“可稳定使用”。

### 2026-08-15 功能 1–4 完成批注

- ✅ **本机社区发现、命名与折叠**：关系网使用本地已观察连线执行确定性的社区检测；孤立点不参与圈层条；圈层名称、折叠状态和当前筛选按账号保存在浏览器本机。真实会话当前从 162 个已连接节点中识别出 11 个有效圈层；折叠 44 人圈层后显示节点由 162 降为 118，展开后恢复。
- ✅ **两位好友最短关系路径**：可从当前可见好友中选择起点与终点，使用 BFS 查找本机已观察关系中的最短路径，并高亮路径节点和连线。缺边只代表本机尚未观察到，不把“无路径”解释为现实中没有好友关系。
- ✅ **标签与跨圈筛选**：关系网可按已有本机标签筛选，并可切换为只显示跨社区连接；跨圈筛选不重新执行上游扫描。真实会话中显示边由 2074 条收敛为 1243 条，关闭后恢复。当前账号没有本机标签，因此标签筛选只完成组件、类型和空状态验证，尚无真实标签数据验收。
- ✅ **好友工作台**：好友页已从概览卡片升级为独立工作台，支持名称/ID/备注/分组/标签搜索，状态、平台、标签筛选及多种排序；宽屏使用双列固定行高虚拟滚动，窄屏自动单列，并按账号保存筛选、排序和滚动位置。真实会话 186 位好友首屏只渲染 34 个条目，滚动窗口切换有效。
- ✅ **本轮工程验证**：前端 19 项 Vitest 测试、`vue-tsc` 类型检查和 Vite 生产构建通过；已在当前登录的 Edge 会话中完成上述只读交互验收。Windows 开发包已重新生成并启动在 `127.0.0.1:47831`。

### 2026-08-15 关系网中心密度优化

- ✅ 保留“核心密集、外围稀疏”的布局方向，提高高密度图的节点斥力、理想连线长度和随节点数增长的碰撞间距，不把关系图平均摊平。
- ✅ 手动拖动并保存的坐标不再无条件覆盖碰撞结果；非拖动状态会让手动节点避开自动节点，拖动期间仍使用轻量坐标更新，松开后才执行一次防重叠整理，避免重新引入拖动卡顿。
- ✅ 162 节点真实会话对比：头像重叠由 1 对降为 0，光环重叠由 1 对降为 0，最小头像净间距由 `-17.99` 提高到 `22.00` 个图坐标单位。
- ✅ 默认名称由 43 个降为 10 个；聚焦时只追加当前好友与连接数最高的 8 位邻居名称，减少文字遮挡但保留焦点信息。
- ✅ 前端测试增至 20 项，Vitest、TypeScript 检查和生产构建通过；已重新打包并在当前登录 Edge 中复测。

### 2026-08-15 Edge 高倍缩放闪烁与黑色遮罩修复

- ✅ 根因定位为 SVG 合成路径：2074 条连线仍参与鼠标命中、焦点切换时 Vue 会批量修改全部节点并增删文字元素，同时每个头像各自维护一个 SVG `clipPath`；Edge 在高倍缩放和高连接节点聚焦时可能让头像裁剪层整层失效，表现为黑色遮罩或头像瞬间消失。
- ✅ 连线改为不参与指针命中，避免鼠标在头像边缘与连线之间反复触发进入/离开。
- ✅ 头像由 162 个独立裁剪定义改为一个 `objectBoundingBox` 圆形裁剪定义，降低 Edge 的 SVG 资源与合成负担。
- ✅ 名称节点改为稳定常驻，使用 CSS 控制可见性；焦点节点和实际邻居改为索引驱动的增量 DOM class 更新，不再让 Vue 在每次悬停时遍历重建整组 SVG 节点。
- ✅ 当前真实图在 195% 缩放下分别对低连接节点和 109 条连接的核心节点连续执行 16 次聚焦检查：162 张头像保持渲染，无节点透明度压暗、无固定遮罩层，焦点连线与最多 9 个名称正常显示。

### 2026-08-15 相交朋友圈语义改造

- ✅ 原“本机圈层”改为“相交朋友圈”，默认名称采用“你与某位好友的朋友圈”，弱化生硬的算法社区编号。
- ✅ 每个朋友圈的主题好友按该圈内部的已观察连接数选择；圈外连接不会参与主题排名，相同连接数时按显示名称与用户 ID 稳定排序。
- ✅ 朋友圈条目展示主题好友头像、主题名称、圈内连接数和成员数；点击主体只看该朋友圈，折叠按钮继续控制整圈显示。
- ✅ 仍明确标注数据来自本机已观察关系；“相交朋友圈”表示你与主题好友在当前可见好友关系中的局部交集，不推断未观察到的 VRChat 好友关系。
- ✅ 新增主题选择单元测试，前端测试增至 21 项，类型检查与生产构建通过。重启后会话曾短暂显示上游不可用，随后自动恢复；已在当前真实登录态验证 162 个节点、2269 条连线和 7 个相交朋友圈均正常渲染。

### 2026-08-15 第一批日常闭环功能

- ✅ **好友档案 2.0**：好友抽屉新增近 30 天本机观测事件、覆盖天数、已配对同场时长、最近同场、活跃时段、常见世界和最近时间线。时长只累计能同时匹配 `game.player-joined` 与 `game.player-left` 的区间，超过 12 小时的异常区间不计入；数据为空时明确说明“未观测到不代表没有共同活动”。本机聚合与上游资料请求分项到达即渲染，不再被慢速共同好友接口整体阻塞。
- 🟡 **发现中心与单人好友请求**：新增 VRChat 用户搜索、完整 `usr_` ID 读取、本机日志最近遇到列表、好友状态检查，以及逐人确认后的好友请求。搜索结果不再自动为多人查询关系状态；只有用户点击某一位时才检查并决定是否允许提交，避免一次搜索形成请求风暴。当前已覆盖搜索与本机日志来源；公共实例成员和群组成员来源仍受可见字段与权限限制，留待实例/群组模块补齐。
- ✅ **今日动态首页**：总览改为紧凑的今日工作台，汇总可加入好友、好友聚集世界、未读通知、收藏数量和最近活动；好友、世界、通知、历史等数据分项到达即更新，不再等待全部启动请求结束后一次性显示。
- ✅ **相交朋友圈演变**：浏览器按账号本机保存最多 30 个实际变化快照，支持当前图与历史快照差量、跨圈关键好友排名和两个朋友圈成员交集比较。变化文案统一使用“新观察到/当前未观察到”，不把缺边表述为解除好友。
- ✅ **本轮工程验证**：Go 全包测试通过；前端 23 项 Vitest、TypeScript 检查和 Vite 生产构建通过。真实会话已验证 186 位好友、单好友 7 条本机事件的聚合接口、用户搜索与好友状态读取；关系网当前 164 个连接节点、2457 条连线，演变面板显示 3 个分析区块和 8 位跨圈关键好友。未实际发送好友请求，避免在验收中产生上游写操作。

### 2026-08-15 第二批功能实现批注

- ✅ **群组中心**：新增独立群组入口，读取当前账号公开可见的已加入群组，并按选中群组按需读取最近 50 条公告与当前群组实例；群组、公告和实例分别缓存，实例使用 2 分钟短 TTL。权限不足或接口不可用时不推断成员数据，也不绕过 `403`。
- ✅ **头像收藏柜**：新增当前账号 VRChat 头像收藏列表，支持名称/作者/ID/本机标签搜索、PC/Quest/iOS 平台筛选、性能等级筛选和本机标签整理。只处理元数据与经安全代理缓存的图片，不下载 Avatar AssetBundle，也不读取他人私有头像。
- ✅ **世界回忆与本机推荐**：世界页从 30 天本机活动事件聚合最近世界、事件次数和最后观测时间；推荐只在当前已加载世界与本人收藏中排序，明确标注为本机推导，不上传活动历史。
- ✅ **保存筛选方案**：好友工作台可命名保存最多 12 组搜索、状态、平台、标签和排序组合，可应用与删除；方案按 VRChat 账号隔离保存在浏览器本机。
- ✅ **最近访问**：总览新增快速返回区，记录最近打开的好友与世界详情，按账号保存最多 12 项；不保存私密 instance nonce。
- 🟡 **国内线路与缓存控制台 2.0**：缓存中心新增世界、群组、头像实体快照分类计数；后端具备只清理可重新下载实体快照的受 CSRF 保护接口。图片缓存清理继续保留逐次确认。24 小时请求量、命中率、403/429、LRU 和诊断包仍未完成。
- ✅ **本轮工程验证**：新增群组、公告、实例与头像响应解析测试；Go 全包测试、前端 23 项 Vitest、TypeScript 检查和 Vite 生产构建通过。真实账号只读 UI 烟测需要在新 EXE 重启后完成，未执行任何群组、好友、收藏或邀请写操作。
- ✅ **真实账号只读验收补充**：当前会话读取 12 个群组和 50 个收藏头像；抽查群组公告/实例的合法空状态，世界页渲染 8 条回忆和 5 条本机推荐，总览最近访问可回到刚打开的世界。头像网格 50 项中 49 张缩略图成功载入，1 个上游图片资源失效；页面无错误横幅、无横向溢出。未执行任何上游写操作。

### 2026-08-15 第三批原创日常闭环

- ✅ **今晚罗盘**：新增独立“罗盘”入口，从当前可加入好友的真实 instance location 聚合候选路线，融合世界缓存与 VRChat 收藏形成可解释评分；最多展示 3 条，并明确好友人数、候选好友、缓存缺失、生成时间和 30 分钟有效期。不会使用历史位置冒充实时位置。
- ✅ **本机社交查询**：实现五类本地意图——很久没见但当前在线、周五晚常遇好友、跨朋友圈关键好友、与指定好友的常见世界、数据覆盖度。查询只使用当前浏览器中的本机事件和 SQLite 关系快照；跨圈查询在关系网尚未加载时只读取本机图，不发起共同好友扫描。无法识别的问题返回示例，不生成答案。
- ✅ **重逢雷达**：仅使用带世界或玩家进出语义的本机事件判断“遇见”，默认阈值为 7 天，查询中明确写“30 天”时使用 30 天阈值；只从当前在线好友中产生候选。支持“保持联系”和“暂不提醒”，偏好按账号保存在浏览器本机，默认不展示私人位置。
- ✅ **群组日历只读接入**：群组详情在选中单个群组时读取该群组当月最多 100 个可见 Calendar 事件，缓存 30 分钟，并展示开始时间、重复类型、关注状态和感兴趣人数；不在登录时全量轮询所有群组，也不提供创建、修改、关注或报名写操作。
- ✅ **统一证据边界**：所有罗盘/查询卡片包含分数、理由、覆盖天数、生成时间、过期时间和好友/世界回查目标。事件条数不会被包装成共同会话次数，缺少观测不会被解释为现实中没有活动。
- ✅ **本轮验证**：前端增至 27 项 Vitest，TypeScript 检查、Vite 生产构建及 Go 全包测试通过。真实账号渲染 3 条今晚路线、397 条本机事件/2 个覆盖日、8 位跨圈关键好友；12 个群组正常显示，抽查 3 个群组日历均返回合法当月空结果。页面无错误横幅和横向溢出，未执行 VRChat 写操作。

### 2026-08-15 第四期：记忆与组织

- ✅ **社交潮汐**：将已有关系网快照划分为相交朋友圈，排除孤立点，并按本机时区聚合为星期 × 4 小时时段热力图；朋友圈名称沿用圈内连接最多者作为主题。事件数、覆盖天数和峰值时段均来自本机活动记录，不对缺失时段平滑补数。
- ✅ **世界护照**：从带 `worldId` 的本机活动事件生成首次/最近记录、事件数和同行好友；世界名称与封面仅从当前已加载世界或收藏缓存补全。印象、标签和笔记按 VRChat 账号保存在当前浏览器，暂未自动扫描截图目录。
- ✅ **好友动向与朋友圈交汇**：已移除使用价值较低的“活动编队”。当前位置按 instance 合并，历史事件按 20 分钟窗口与世界/状态合并，不再逐条堆叠；每个场景结合共同好友关系计算参与朋友圈、跨圈连线与关键连接人。私人位置、未返回位置和未观察时段明确归为未知。
- ✅ **中国线路起航检查**：统一展示本机数据库、VRChat REST、Pipeline、系统/自有代理模式、媒体缓存和目标世界历史线索，并计算仅供本机参考的就绪分。检查不会自动切换代理，也不测试公共代理池。
- ✅ **工程与真实会话验证**：新增 3 项纯函数测试，前端总计 30 项 Vitest、`vue-tsc`、Vite 生产构建与 Go 全包测试通过。Windows 单 EXE 已重启；当前真实登录态渲染 10 个有连接朋友圈、433 条本机活动证据和 50 个世界护照记录，6 层起航检查为正常，Pipeline 已连接，媒体缓存 565 个文件。390×844 页面无整体横向溢出。50 个历史世界中仅 4 个在当前缓存中有可用封面，未缓存项明确显示占位和世界 ID，没有为补图一次性发起 50 个上游请求。

### 2026-08-15 关系网缩放稳定性与视觉改版

- ✅ **移除 SVG 遮罩路径**：关系网头像不再引用共享 `clipPath`。图片从本机媒体代理加载后，先在浏览器 Canvas 中裁成带透明圆角的 WebP，再作为普通 SVG 图片显示；Canvas 失败时回退为不裁剪的原图，不再创建 SVG 遮罩或裁剪器。
- ✅ **多倍率真实回归**：当前 Edge 登录态在 80%、100%、125% 和 195% 图内缩放下均显示 164 个节点与 164 张头像；全部头像使用本机生成的 `data:image`，DOM 中 `clipPath` / `mask` 数量为 0。四个倍率均无黑色遮罩、头像整层消失或错误横幅。
- ✅ **文案减负**：全局页面副标题和关系网页头、统计、朋友圈、筛选、路径、拖动提示改为简短任务语言；实现边界只在需要确认、清理或解释空数据时出现，不再每块重复说明。
- ✅ **暖调主题**：浅色主题改为暖白、燕麦灰与咖啡棕；深色主题改为暖石墨、香槟棕与低饱和绿。关系网圈层颜色同步降低蓝紫占比，保留状态辨识度。
- ✅ **验证**：前端 30 项 Vitest、TypeScript 检查、Vite 生产构建和 Go 全包测试通过；浅色与深色真实会话截图均完成检查。

### 2026-08-15 好友动向聚合

- ✅ **活动编队下线**：入口、Vue 状态、创建/删除/成员维护逻辑均已移除；旧浏览器计划键不再读取，也未主动清除，避免无提示删除本机数据。
- ✅ **实时场景**：在线好友按当前可见 instance 聚合，展示同场人数、所属朋友圈、跨圈关系和关键连接人；位置不可见者只计入“位置未知”。
- ✅ **近期场景**：活动事件按 20 分钟与世界/状态合并，显示合并后的好友数和事件数，避免把上线、离线和移动日志逐条铺满页面。
- ✅ **交织关系**：从实时与近期场景统计朋友圈组合的同场次数、涉及好友、世界和跨圈连线，并给出可回查的连接人。
- 🟡 **覆盖边界**：当前只能呈现 VRChat 返回的好友状态与应用运行期间的本机观测，不能看到私人位置、隐身状态或应用未运行时的完整轨迹；页面已直接展示可见/未知数量与历史覆盖天数。
- ✅ **工程与真实会话验证**：前端 31 项 Vitest、TypeScript、Vite 生产构建和 Go 全包测试通过；Windows 单 EXE 已重启。真实账号当前为 37 位在线、13 位位置可见、24 位位置未知，聚合成 11 个实时地点、173 组近期场景和 36 组朋友圈交汇（界面展示前 8 组）；38 张可见图片全部加载，无错误横幅和横向溢出。

### 2026-08-15 动向资料补全与可读性

- ✅ **实时世界补全**：好友动向进入可见地点页后，只对当前缺少资料的 `wrld_` ID 做按需、串行补全；不会在登录时扫描全部历史世界，也不会并发形成请求风暴。真实登录态 9 个实时地点在 15 秒低频补全后占位标题由 6 个降为 0，世界名称和封面均正常显示。
- ✅ **交汇可读性**：动向页的场景标题、人数、双方朋友圈名称、连接人和摘要字号均已提高；右侧交汇栏最小宽度提高到 400px。当前 Edge 实测交汇双方 CSS 字号为 13px，叠加默认 115% 界面比例后可直接阅读，页面宽度 1585px/视口 1600px，无整体横向溢出。
- ✅ **界面比例控制**：新增 90%–140%、5% 步进的界面大小滑轨，默认 115%，同时缩放文字、按钮与头像。设置按浏览器本机保存，不写入 VRChat 账号。
- 🟡 **中英文切换**：左侧提供即时切换按钮，当前已完成导航外壳、页面标题、显示设置、旅程与好友动向主视图双语；其他业务详情页仍有中文固定文案，因此实现程度为部分完成，后续按组件逐页补齐，不使用在线翻译服务。
- ✅ **我的资料编辑**：左下角本人头像和名称可打开资料编辑器，读取当前账号最新资料，可修改在线状态、状态签名、称谓、个人简介、最多 3 条链接和头像复制偏好。保存前二次确认，经本机网关分别写入当前会话用户的 `users/{id}` 与 `profile/{id}`，不能指定或修改他人 ID。
- ✅ **工程与真实会话验证**：前端 33 项 Vitest、TypeScript、Vite 生产构建和 Go 全包测试通过；Windows 单 EXE 已重新生成并启动。真实账号只读验收确认 44 位在线、16 位位置可见、9 个实时地点、62 张图片成功加载、0 张破图、0 个世界占位标题；个人资料编辑器读取字段成功但未点击保存，未产生上游写操作。

### 2026-08-15 好友 Boop 与邀请修复

- ✅ **单人 Boop**：好友详情新增 6 个常用内置表情，发送前二次确认；浏览器只调用受 CSRF 保护的本机网关，网关校验用户 ID 和 `default_` / `file_` 表情 ID 后调用 `POST /users/{userId}/boop`。未提供批量或后台自动发送。
- ✅ **邀请请求修复**：移除错误写入的 `Authorization: application/json`，请求体按当前接口只发送实例标签 `instanceId`，继续使用 CookieJar 登录会话。
- ✅ **403 提示修复**：不再把所有 403 误报为“中国线路出口被拒绝”；优先展示 VRChat 返回信息，无明确信息时提示检查好友关系、实例权限或功能设置，且不再将普通 403 标为可重试网络故障。
- ✅ **自动化验证**：Go 全包测试通过；前端增至 34 项 Vitest，TypeScript 检查和 Vite 生产构建通过。尚未向真实好友发送 Boop 或邀请，真实上游写操作等待用户自行验收。

### 2026-08-15 0.9.0-beta.1 发布收口

- ✅ **产品与版本统一**：正式产品名改为 `VRC++`；界面、浏览器标题、网关、托盘、安装目录、快捷方式、可执行文件、安装程序、npm 包标识和发布文档已统一。内部 `VRC_HARBOR_*` 环境变量及 `vrc-harbor-*` 浏览器存储键保留兼容，避免现有配置和本机布局失效。
- ✅ **品牌资源**：用户提供的 `酥咔1.png` 仅做本地高质量缩放，生成 256px 应用 Logo 和 64px favicon；登录页及登录后侧边栏真实渲染正常，静态资源均由单 EXE 本机提供。
- ✅ **开发内容下线**：登录页“实施蓝图”入口已移除，网关对 `/docs/*` 和 `.md` 返回 404；Vite public 目录从 `docs` 改为正式 `public`，发布构建会清空旧产物，不再把开发文档或历史 JS/CSS 带入 EXE。
- ✅ **发布工程收口**：版本由根 `package.json` 驱动构建和打包；更新比较改为语义化版本排序，避免同版本、旧版本和预发布版本误判；默认数据目录改为 `%AppData%\VRC++`，同时兼容已存在的旧开发目录。
- 🟡 **候选安装程序**：当前候选为 `dist/VRC++-Setup-0.9.0-beta.2.exe`。Go 全包测试、39 项 Vitest、TypeScript、Vite 构建和当前电脑的静默安装已通过，开始菜单入口与卸载信息正常；仍需完成签名后的线上覆盖升级和卸载验收。
- 🟡 **正式发布阻塞**：当前 Windows 环境未找到 `signtool.exe`，也未配置可信代码签名证书，候选 EXE 状态为 `NotSigned`。自动检测已经可用，但正式更新下载会拒绝未签名安装程序；真实邀请/Boop、干净 Windows 安装和线上双源更新仍需人工验收。
